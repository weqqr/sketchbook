use crate::error::*;
use crate::math::*;
use std::fs::File;
use std::io::BufRead;
use std::path::Path;


pub fn load<P: AsRef<Path>>(p: P) -> Result<(usize, usize, Vec<Float>)> {
    unimplemented!()
}
